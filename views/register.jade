extend ./layout.jade
block head

block content
    .tera-body.container
        .row
            .panel.panel-register.panel-default.text-center.col-xs-12.col-sm-8.col-md-6.col-lg-4.col-sm-offset-0.col-md-offset-3.col-lg-offset-4
                .panel-body
                    form(action='/register', method="POST")
                        .err
                            | #{errorMessage}
                        fieldset
                            h5
                                | Số điện thoại của bạn sẽ được dùng như tài khoản để đăng nhập
                            br
                            h5
                                | Luôn được bảo mật và chỉ có bạn bè có thể liên lạc với bạn trong Tera
                            br
                        .form-group
                            input.form-control.input(type='number', name='phone', value=phone || '' ,placeholder='Vui lòng nhập số điện thoại di động thường dùng')
                        .form-group
                            label.control-label(for='btnSend')
                            button#btnSend.btn.btn-danger(type='submit', name='send', aria-label='')
                                | Tiếp tục


block page-script
    style.
        .tera-body.container {
            margin-top: 100px;
        }
    script.

